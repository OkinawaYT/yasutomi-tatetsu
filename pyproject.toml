[project]
name = "researchmap-updater"
version = "0.1.0"
description = "A tool to update ResearchMap profiles using API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.32",
]